#!/usr/bin/env node
// Minimal placeholder smoke script used by CI until a more robust smoke test runner exists.
// It is deterministic and returns success. Replace with real smoke checks when ready.

console.log('Placeholder smoke checks: none configured. Skipping.');
process.exit(0);
